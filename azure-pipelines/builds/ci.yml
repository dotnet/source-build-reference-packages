trigger:
  batch: true
  branches:
    include:
    - master
    - release/*
    - dev/source-build-5.0

pr:
  branches:
    include:
    - master
    - release/*
    - dev/source-build-5.0

stages:
- stage: build
  displayName: Build
  jobs:
  - template: /eng/common/templates/job/build-from-source.yml
    parameters:
      managedOnly: true
