<Project>

  <Import Project="$([MSBuild]::GetPathOfFileAbove(Directory.Build.props, $(MSBuildThisFileDirectory)..))" />

  <PropertyGroup>
    <AssemblyName>Microsoft.VisualStudio.Setup.Configuration.Interop</AssemblyName>
  </PropertyGroup>

</Project>
